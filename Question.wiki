= 问题 =

* 服务器在处理消息时 每次都会调用makeChange
  在处理handle时会记录到底有哪些修改 
  然后统一在makeChange中再次看到底哪修改了再发给客户端 
  虽然所有的修改都走同样的接口了 但是逻辑太耦合了吧 
  而且效率也低 不知道为什么不能直接把修改的结果发送给客户端
  - 原因还是在于想把所有东西放在一块 这样处理不会分散到处
  - 但是还是觉得这样并不是很好的做法 如果功能多且复杂起来 并不是一个好维护的方案

* 如何调整ping包时长 方便测试
* gm命令需要后台添加 太麻烦
